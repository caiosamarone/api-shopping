import styled from 'styled-components'

export const Wrapper = styled.main`
    display: grid;
    grid-template-columns: repeat(3, 1fr); /* 3 colunas e cada uma deve ter uma fraçao */
    grid-column-gap: 24px; /** da um espacamento entre as colunas  */

    padding: 24px;  
`

